/*function readFunc()
{
  var fs = require("fs");
  var str = fs.readFileSync(file, 'utf8');
  fs.writeFile(outputDoc, str, function(err)
   {
    if(err) {
        return console.log(err);
   }

    console.log("");
}
;

var fs = require('fs');
fs.writeFile(outputDoc, "Hey there!", function(err) {
    if(err) {
        return console.log(err);
    }

    console.log("The file was saved!");
});
// readFunc will output data to
*/
